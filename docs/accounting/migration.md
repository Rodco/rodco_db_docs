#### product_cost <- costoHistorico

* id <- id
* create_at <- create_at
* cost <- costo
* product_id <- productoId
* initial_inventory <- inventarioInicial
* previous_cost <- costoAnterior
* active <- activo